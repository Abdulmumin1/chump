---
"chump-agent": patch
---

Expose instruction files and skills in health/status endpoints, and render loaded resources on startup. Includes helper utilities to display resource paths in terminal UI.