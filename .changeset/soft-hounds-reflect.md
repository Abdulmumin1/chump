---
"chump-agent": patch
---

Fix web transcript replay and steering behavior so live sessions stay event-sourced, avoid stale snapshot overrides, and queue messages through the normal send action while an agent is running.
